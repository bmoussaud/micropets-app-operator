#@ load("@ytt:data", "data")
---
apiVersion: carto.run/v1alpha1
kind: ClusterDeploymentTemplate
metadata:
  name: deploy-apply
spec:
  observedCompletion:
    succeeded:
      key: '.status.conditions[?(@.type=="ReconcileSucceeded")].status'
      value: 'True'
    failed:
      key: '.status.conditions[?(@.type=="ReconcileSucceeded")].status'
      value: 'False'
      
  template:
    apiVersion: kappctrl.k14s.io/v1alpha1
    kind: App
    metadata:
      name: $(deliverable.metadata.name)$
    spec:
      serviceAccountName: #@ data.values.service_account_name
      fetch:       
        - http:
            url: $(deployment.url)$            
            subPath: applications/$(params.application)$
        - http:
            url: $(deployment.url)$            
            subPath: environments/$(params.environment)$
      template:
        - ytt: 
            ignoreUnknownComments: true                               
      deploy:
        - kapp:
            rawOptions: ["--diff-changes=true"]
